# Configuration Nginx pour déployer le frontend et backend
# Placez ce fichier dans /etc/nginx/sites-available/miscadin
# Créez un lien symbolique : sudo ln -s /etc/nginx/sites-available/miscadin /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name votre-domaine.com www.votre-domaine.com;

    # Redirection HTTPS (décommentez après avoir configuré SSL)
    # return 301 https://$server_name$request_uri;

    # Frontend (React)
    location / {
        root /var/www/miscadin/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Fichiers statiques
    location /static {
        alias /var/www/miscadin/dist;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Configuration HTTPS (après avoir installé SSL avec Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name votre-domaine.com www.votre-domaine.com;
#
#     ssl_certificate /etc/letsencrypt/live/votre-domaine.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/votre-domaine.com/privkey.pem;
#
#     # Frontend
#     location / {
#         root /var/www/miscadin/dist;
#         try_files $uri $uri/ /index.html;
#     }
#
#     # Backend API
#     location /api {
#         proxy_pass http://localhost:5000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

